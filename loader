local cloneref = cloneref or function(x) return x end

local game          = cloneref(game)
local http_service  = cloneref(game:GetService("HttpService"))
local players       = cloneref(game:GetService("Players"))
local starter_gui   = cloneref(game:GetService("StarterGui"))


--@types
type loader_config = {
	name: string,
	url: string,
	version: string?,
	author: string?,
}

--@list_game_loaders
local ___loaders: { [number]: loader_config } = {
	[6701277882] = {
		name    = "fish it",
		url     = "https://luaegis.net/scripts/v4/loaders/5825dfde-5490-47f1-9af0-9da0ad0a7cb5.lua",
		version = "1.0.0",
		author  = "Andra",
	},
}
--@utility
local function __notify(title: string, text: string): ()
	pcall(function()
		starter_gui:SetCore("SendNotification", {
			Title    = title,
			Text     = text,
			Duration = 5,
		})
	end)
end
--@loader
local function __load_script(url: string): boolean
	local success: boolean, result: any = pcall(function()
		local script_content: string = game:HttpGet(url)
		local loaded_function: any = loadstring(script_content)

		if loaded_function then
			loaded_function()
			return true
		else
			error("failed to compile script")
		end
	end)

	if not success then
		warn("[loader] failed:", result)
		__notify("Vora loader", "failed to load script")
		return false
	end

	return true
end
--@main loader
local place_id: number    = game.PlaceId
local universe_id: number = game.GameId
local loader_config: loader_config = ___loaders[place_id]
	or ___loaders[universe_id]
	or error("no loader configured for this game")
    
--@main execution
__load_script(loader_config.url)
